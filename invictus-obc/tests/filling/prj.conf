# tests/prj.conf

# Enable Zephyr’s testing framework
CONFIG_ZTEST=y

# Optionally, adjust logging level
CONFIG_LOG=y
CONFIG_LOG_DEFAULT_LEVEL=4

# State Machine Framework settings
CONFIG_SMF=y
CONFIG_SMF_ANCESTOR_SUPPORT=y
CONFIG_SMF_INITIAL_TRANSITION=y
