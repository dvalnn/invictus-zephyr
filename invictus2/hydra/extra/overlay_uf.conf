CONFIG_HYDRA_UF=y
CONFIG_HYDRA_LF=n
CONFIG_HYDRA_FS=n

# Optional: expose a readable string for logs
# (This derives from Kconfig defaults but can be forced if needed)
# CONFIG_HYDRA_VARIANT="upper feed"
