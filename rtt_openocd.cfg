source [find interface/cmsis-dap.cfg]
adapter speed 5000
set USE_CORE 0
source [find target/rp2040.cfg]

init
reset init

# Set up RTT at the known RAM address (adjust if needed)
rtt setup 0x20000410 1024 "SEGGER RTT"
rtt start
rtt server start 9090 0

# Keep OpenOCD running
halt
